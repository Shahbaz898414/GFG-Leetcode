#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int solve(vector<int> a){
  sort(a.begin(),a.end());
        
       
        for (int i = 0; i <a.size(); i++)
        {
          /* code */
        }
        
        return 0;
}
int main() {
	 
  vector<int> n;

  for (int i = 0; i < n.size(); i++) {
    /* code */
    int x;
    cin>>x;
    n.push_back(x);
  }

  cout<<solve(n)<<endl;
  
   
	return 0;
}